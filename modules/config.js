// Configuration 

const rootURL = 'https://assets.cmcmarkets.com';

const config = {
    rootURL: rootURL,
    popularRoute: `${rootURL}/json/cmc-test-most-popular-feed.json`,
    forexRoute: `${rootURL}/json/cmc-test-forex-feed.json`,
}

export default config ;